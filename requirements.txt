# Core runtime dependencies for the HonestCode MCP server.
# Lightweight: no torch / transformers / tree-sitter required — the
# server parses Python with the standard-library `ast` module.
mcp>=1.6.0
pyyaml>=6.0
tomli>=2.0.0; python_version<"3.11"
